name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing tag to release or recover
        required: true
        type: string

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: write

env:
  CARGO_TERM_COLOR: always
  CRATES_IO_USER_AGENT: ruviz-release-workflow/1.0 (https://github.com/Ameyanagi/ruviz)
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
  RELEASE_REF: refs/tags/${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}

jobs:
  check-ci:
    name: Wait for CI
    runs-on: ubuntu-latest
    outputs:
      release_sha: ${{ steps.release-target.outputs.release_sha }}
    steps:
      - name: Checkout workflow utilities
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - name: Manual recovery - skip tag-push CI gate
        if: github.event_name == 'workflow_dispatch'
        run: echo "Manual recovery for ${RELEASE_TAG} explicitly skips the tag-push CI gate."

      - name: Resolve release target and enforce tag-push CI gate
        id: release-target
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_PUSHED_AT: ${{ github.event.repository.pushed_at }}
        run: |
          args=(
            --repo "${GITHUB_REPOSITORY}"
            --workflow "ci.yml"
            --tag "${RELEASE_TAG}"
            --ref "${RELEASE_REF}"
            --timeout 2400
            --interval 15
          )

          if [[ "${GITHUB_EVENT_NAME}" == "push" ]]; then
            args+=(--sha "${GITHUB_SHA}" --pushed-at "${RELEASE_PUSHED_AT}")
          else
            args+=(--skip-ci)
          fi

          python3 scripts/wait_for_github_checks.py "${args[@]}"

  build-release:
    name: Build Release
    runs-on: ubuntu-latest
    needs: check-ci
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Ensure wasm target is installed for the pinned toolchain
        run: rustup target add wasm32-unknown-unknown

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Validate release tag matches package versions
        run: |
          TAG="${{ env.RELEASE_TAG }}" python3 <<'PY'
          import json
          import os
          import re
          import sys
          import tomllib
          from pathlib import Path

          tag = os.environ["TAG"]
          match = re.fullmatch(r"v(?P<base>\d+\.\d+\.\d+)(?:-(?P<label>a|b|rc)(?P<num>\d+))?", tag)
          if match is None:
              print(
                  f"Unsupported release tag format: {tag}. Expected vX.Y.Z or vX.Y.Z-(a|b|rc)N",
                  file=sys.stderr,
              )
              sys.exit(1)

          base = match.group("base")
          label = match.group("label")
          number = match.group("num")
          cargo_expected = base if label is None else f"{base}-{label}{number}"
          python_expected = base if label is None else f"{base}{label}{number}"

          with Path("packages/ruviz/package.json").open(encoding="utf-8") as fh:
              npm_web_version = json.load(fh)["version"]

          with Path("Cargo.toml").open("rb") as fh:
              ruviz_version = tomllib.load(fh)["package"]["version"]

          with Path("bindings/wasm/Cargo.toml").open("rb") as fh:
              rust_web_version = tomllib.load(fh)["package"]["version"]

          with Path("adapters/gpui/Cargo.toml").open("rb") as fh:
              gpui_version = tomllib.load(fh)["package"]["version"]

          with Path("adapters/gui/Cargo.toml").open("rb") as fh:
              gui_workspace_version = tomllib.load(fh)["workspace"]["package"]["version"]

          gui_adapter_versions = {}
          for adapter in ("ruviz-egui", "ruviz-iced", "ruviz-slint"):
              with Path(f"adapters/gui/{adapter}/Cargo.toml").open("rb") as fh:
                  manifest = tomllib.load(fh)
              package_version = manifest["package"].get("version")
              if package_version == {"workspace": True}:
                  package_version = gui_workspace_version
              gui_adapter_versions[f"{adapter} crate"] = package_version

          with Path("bindings/python/pyproject.toml").open("rb") as fh:
              python_package_version = tomllib.load(fh)["project"]["version"]

          with Path("bindings/python/Cargo.toml").open("rb") as fh:
              python_crate_version = tomllib.load(fh)["package"]["version"]

          versions = {
              "ruviz crate": ruviz_version,
              "ruviz-web crate": rust_web_version,
              "ruviz-gpui crate": gpui_version,
              "ruviz npm package": npm_web_version,
              "ruviz-py crate": python_crate_version,
              **gui_adapter_versions,
          }

          for name, version in versions.items():
              if version != cargo_expected:
                  print(
                      f"{name} version {version} does not match release tag {tag} "
                      f"(expected {cargo_expected})",
                      file=sys.stderr,
                  )
                  sys.exit(1)

          if python_package_version != python_expected:
              print(
                  f"python package version {python_package_version} does not match release tag {tag} "
                  f"(expected {python_expected})",
                  file=sys.stderr,
              )
              sys.exit(1)
          PY

      # The step above proves the manifests match the tag; this one proves the
      # prose does too. Without it a release ships guides that still tell
      # readers to install the previous version, which is how v0.7.0 went out
      # with `ruviz = "0.6.0"` across the README, QUICKSTART and seven guides.
      - name: Validate documented install snippets match the release
        run: |
          python3 <<'PY'
          import importlib.util
          import sys
          from pathlib import Path

          spec = importlib.util.spec_from_file_location(
              "check_docs", Path("scripts/check_docs.py")
          )
          check_docs = importlib.util.module_from_spec(spec)
          # Registered before exec: the module defines a dataclass, which
          # resolves its own module out of sys.modules while being built.
          sys.modules[spec.name] = check_docs
          spec.loader.exec_module(check_docs)

          errors = check_docs.check_documented_version_pins()
          for error in errors:
              print(error, file=sys.stderr)
          sys.exit(1 if errors else 0)
          PY

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            adapters/gui/target
            adapters/gpui/target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('Cargo.lock', 'adapters/gui/Cargo.lock', 'adapters/gpui/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-

      - name: Cache Bun
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: Install JS workspace dependencies
        run: bun install --frozen-lockfile

      - name: Check JS/TS formatting and lint
        run: bun run check:web

      - name: Install GPUI Linux build dependencies
        uses: ./.github/actions/install-linux-desktop-build-deps

      - name: Build release
        run: cargo build --release --all-features

      - name: Verify ruviz-web 3D GPU wasm release surface
        run: cargo check -p ruviz-web --target wasm32-unknown-unknown --no-default-features --features 3d-gpu --locked

      # Tests already run in CI workflow (check-ci job waits for it)

      - name: Build documentation
        run: cargo doc --no-deps --all-features

      # ruviz-gpui is its own Cargo workspace, so that the zed-pinned
      # [patch.crates-io] gpui override stops applying to every root cargo
      # command. It is not a root member: reach it by manifest path.
      - name: Verify ruviz-gpui compiles
        run: cargo check --manifest-path adapters/gpui/Cargo.toml --locked

      - name: Verify ruviz-gpui examples compile
        run: |
          cargo check --manifest-path adapters/gpui/Cargo.toml --examples --locked
          cargo check --manifest-path adapters/gpui/Cargo.toml --example plot3d_embed --all-features --locked

      - name: Test and build ruviz-gpui documentation
        run: |
          cargo test --manifest-path adapters/gpui/Cargo.toml --doc --locked
          cargo test --manifest-path adapters/gpui/Cargo.toml --doc --all-features --locked
          cargo doc --manifest-path adapters/gpui/Cargo.toml --all-features --no-deps --locked
        env:
          RUSTDOCFLAGS: -D warnings

      - name: Verify native GUI adapters and examples compile
        run: |
          cargo check --manifest-path adapters/gui/Cargo.toml --workspace --all-features --locked
          cargo check --manifest-path adapters/gui/Cargo.toml --workspace --examples --all-features --locked

      - name: Build native GUI adapter documentation
        run: cargo doc --manifest-path adapters/gui/Cargo.toml --workspace --all-features --no-deps --locked
        env:
          RUSTDOCFLAGS: -D warnings

      - name: Validate npm tarball
        run: bun run --cwd packages/ruviz verify:pack

  build-python-sdist:
    name: Build Python sdist
    runs-on: ubuntu-latest
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          maturin-version: v1.12.6
          command: sdist
          args: --out dist
          working-directory: bindings/python

      # The sdist is not smoke tested here: installing it compiles the crate from
      # source and needs the Linux desktop build dependencies, so CI covers that
      # install in the `Python Packaging` job instead.
      - name: Upload Python sdist
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-sdist
          path: bindings/python/dist/*.tar.gz
          if-no-files-found: error

  build-python-wheel-macos-intel:
    name: Build Python wheel (macOS Intel)
    runs-on: macos-15-intel
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build macOS Intel wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          maturin-version: v1.12.6
          rust-toolchain: 1.94.1
          working-directory: bindings/python
          args: --release --out dist

      - name: Smoke test macOS Intel wheel
        run: |
          python3 -m venv "${RUNNER_TEMP}/smoke"
          "${RUNNER_TEMP}/smoke/bin/python" -m pip install --quiet bindings/python/dist/*.whl
          "${RUNNER_TEMP}/smoke/bin/python" bindings/python/scripts/smoke_test_wheel.py \
            --label "macOS Intel wheel"

      - name: Upload macOS Intel wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-macos-intel
          path: bindings/python/dist/*.whl
          if-no-files-found: error

  build-python-wheel-macos-arm:
    name: Build Python wheel (macOS Apple Silicon)
    runs-on: macos-14
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build macOS Apple Silicon wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          maturin-version: v1.12.6
          rust-toolchain: 1.94.1
          working-directory: bindings/python
          args: --release --out dist

      - name: Smoke test macOS Apple Silicon wheel
        run: |
          python3 -m venv "${RUNNER_TEMP}/smoke"
          "${RUNNER_TEMP}/smoke/bin/python" -m pip install --quiet bindings/python/dist/*.whl
          "${RUNNER_TEMP}/smoke/bin/python" bindings/python/scripts/smoke_test_wheel.py \
            --label "macOS Apple Silicon wheel"

      - name: Upload macOS Apple Silicon wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-macos-arm
          path: bindings/python/dist/*.whl
          if-no-files-found: error

  build-python-wheel-windows:
    name: Build Python wheel (Windows)
    runs-on: windows-latest
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build Windows wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          maturin-version: v1.12.6
          rust-toolchain: 1.94.1
          working-directory: bindings/python
          args: --release --out dist

      - name: Smoke test Windows wheel
        shell: pwsh
        run: |
          python -m venv "$env:RUNNER_TEMP\smoke"
          $smoke_python = "$env:RUNNER_TEMP\smoke\Scripts\python.exe"
          $wheel = (Get-ChildItem bindings/python/dist/*.whl | Select-Object -First 1).FullName
          & $smoke_python -m pip install --quiet $wheel
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          & $smoke_python bindings/python/scripts/smoke_test_wheel.py --label "Windows wheel"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload Windows wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-windows
          path: bindings/python/dist/*.whl
          if-no-files-found: error

  build-python-wheel-linux:
    name: Build Python wheel (Linux)
    runs-on: ubuntu-latest
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Build Linux wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          maturin-version: v1.12.6
          rust-toolchain: 1.94.1
          manylinux: 2_28
          target: x86_64-unknown-linux-gnu
          working-directory: bindings/python
          args: --release --out dist --compatibility pypi --interpreter python3.10 --no-default-features --features pyo3/extension-module

      - name: Smoke test Linux wheel
        run: |
          python3 -m venv "${RUNNER_TEMP}/smoke"
          "${RUNNER_TEMP}/smoke/bin/python" -m pip install --quiet bindings/python/dist/*.whl
          "${RUNNER_TEMP}/smoke/bin/python" bindings/python/scripts/smoke_test_wheel.py \
            --label "Linux x86_64 wheel" --no-native-show

      - name: Upload Linux wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-linux
          path: bindings/python/dist/*.whl
          if-no-files-found: error

  build-python-wheel-linux-aarch64:
    name: Build Python wheel (Linux aarch64)
    # Native arm64 runner: the manylinux container builds and the wheel can be
    # smoke tested in place, so no QEMU emulation is needed.
    runs-on: ubuntu-24.04-arm
    needs: [check-ci, build-release, build-python-widget-bundle]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Download canonical widget bundle
        uses: actions/download-artifact@v4
        with:
          name: python-widget-bundle
          path: .

      - name: Build Linux aarch64 wheel
        uses: PyO3/maturin-action@v1
        with:
          command: build
          maturin-version: v1.12.6
          rust-toolchain: 1.94.1
          manylinux: 2_28
          target: aarch64-unknown-linux-gnu
          working-directory: bindings/python
          args: --release --out dist --compatibility pypi --interpreter python3.10 --no-default-features --features pyo3/extension-module

      - name: Smoke test Linux aarch64 wheel
        run: |
          python3 -m venv "${RUNNER_TEMP}/smoke"
          "${RUNNER_TEMP}/smoke/bin/python" -m pip install --quiet bindings/python/dist/*.whl
          "${RUNNER_TEMP}/smoke/bin/python" bindings/python/scripts/smoke_test_wheel.py \
            --label "Linux aarch64 wheel" --no-native-show

      - name: Upload Linux aarch64 wheel
        uses: actions/upload-artifact@v4
        with:
          name: python-dist-linux-aarch64
          path: bindings/python/dist/*.whl
          if-no-files-found: error

  build-python-widget-bundle:
    name: Build canonical Python widget bundle
    runs-on: ubuntu-latest
    needs: [check-ci, build-release]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Ensure wasm target is installed for the pinned toolchain
        run: rustup target add wasm32-unknown-unknown

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-python-widget-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-python-widget-

      - name: Cache Bun
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: Install JS workspace dependencies
        run: bun install --frozen-lockfile

      - name: Build canonical widget bundle
        run: bun run build:python-widget

      - name: Upload canonical widget bundle
        uses: actions/upload-artifact@v4
        with:
          name: python-widget-bundle
          path: bindings/python/ruviz_py/ruviz/widget.js
          if-no-files-found: error

  publish-ruviz:
    name: Publish ruviz to crates.io
    runs-on: ubuntu-latest
    needs: [check-ci, build-release]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Detect published ruviz version
        id: ruviz-version
        run: |
          VERSION="$(cargo pkgid -p ruviz | sed 's/.*[#@]//')"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

          if curl --silent --fail \
              --header "User-Agent: ${CRATES_IO_USER_AGENT}" \
              "https://crates.io/api/v1/crates/ruviz/${VERSION}" >/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish ruviz to crates.io
        if: steps.ruviz-version.outputs.published != 'true'
        run: cargo publish --package ruviz --locked --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Skip ruviz publish
        if: steps.ruviz-version.outputs.published == 'true'
        run: echo "ruviz v${{ steps.ruviz-version.outputs.version }} is already published on crates.io"

  verify-packaged-crates:
    name: Verify packaged ruviz crates
    runs-on: ubuntu-latest
    needs: [check-ci, publish-ruviz]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0

      - name: Cache cargo sources
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-release-packaged-crates-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-packaged-crates-

      - name: Install GPUI Linux build dependencies
        uses: ./.github/actions/install-linux-desktop-build-deps

      - name: Verify packaged crates against crates.io
        run: >-
          uv run python scripts/verify_packaged_crates.py
          --mode release
          --expected-vcs-sha ${{ needs.check-ci.outputs.release_sha }}
          --registry-attempts 60
          --registry-delay 20

  verify-gui-adapter-packages:
    name: Verify packaged native GUI adapters
    runs-on: ubuntu-latest
    needs: [check-ci, build-release, publish-ruviz]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0

      - name: Install Linux desktop build dependencies
        uses: ./.github/actions/install-linux-desktop-build-deps

      - name: Cache GUI adapter package sources
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            adapters/gui/target
          key: ${{ runner.os }}-cargo-release-gui-adapters-${{ hashFiles('adapters/gui/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-gui-adapters-

      - name: Verify packaged adapters against crates.io
        run: >-
          uv run python scripts/verify_gui_adapter_packages.py
          --mode release
          --expected-vcs-sha ${{ needs.check-ci.outputs.release_sha }}
          --registry-attempts 60
          --registry-delay 20

  publish-gui-adapters:
    name: Publish ${{ matrix.crate }} to crates.io
    runs-on: ubuntu-latest
    needs: [check-ci, verify-gui-adapter-packages]
    strategy:
      fail-fast: false
      matrix:
        crate: [ruviz-egui, ruviz-iced, ruviz-slint]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Detect published adapter version
        id: adapter-version
        shell: bash
        run: |
          manifest="adapters/gui/${{ matrix.crate }}/Cargo.toml"
          version="$(cargo pkgid --manifest-path "${manifest}" --package "${{ matrix.crate }}" | sed 's/.*[#@]//')"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

          if curl --silent --fail \
              --header "User-Agent: ${CRATES_IO_USER_AGENT}" \
              "https://crates.io/api/v1/crates/${{ matrix.crate }}/${version}" >/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Linux desktop build dependencies
        if: steps.adapter-version.outputs.published != 'true'
        uses: ./.github/actions/install-linux-desktop-build-deps

      - name: Publish adapter to crates.io
        if: steps.adapter-version.outputs.published != 'true'
        run: >-
          cargo publish
          --manifest-path adapters/gui/${{ matrix.crate }}/Cargo.toml
          --locked
          --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Skip adapter publish
        if: steps.adapter-version.outputs.published == 'true'
        run: echo "${{ matrix.crate }} v${{ steps.adapter-version.outputs.version }} is already published on crates.io"

  publish-ruviz-web:
    name: Publish ruviz-web to crates.io
    runs-on: ubuntu-latest
    needs: [check-ci, publish-ruviz, verify-packaged-crates]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Detect published ruviz-web version
        id: web-version
        run: |
          VERSION="$(cargo pkgid -p ruviz-web | sed 's/.*[#@]//')"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

          if curl --silent --fail \
              --header "User-Agent: ${CRATES_IO_USER_AGENT}" \
              "https://crates.io/api/v1/crates/ruviz-web/${VERSION}" >/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Wait for ruviz-web packaging to resolve against crates.io
        if: steps.web-version.outputs.published != 'true'
        run: |
          last_output=""

          for attempt in $(seq 1 60); do
            if output="$(cargo package --package ruviz-web --allow-dirty --no-verify 2>&1)"; then
              exit 0
            fi

            last_output="${output}"
            echo "Waiting for crates.io index to serve ruviz-web dependencies (attempt ${attempt}/60)..."
            sleep 20
          done

          echo "Timed out waiting for ruviz-web packaging to resolve against crates.io"
          if [ -n "${last_output}" ]; then
            echo "Last cargo package error output:"
            printf '%s\n' "${last_output}"
          fi
          exit 1

      - name: Publish ruviz-web to crates.io
        if: steps.web-version.outputs.published != 'true'
        run: cargo publish --package ruviz-web --locked --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Skip ruviz-web publish
        if: steps.web-version.outputs.published == 'true'
        run: echo "ruviz-web v${{ steps.web-version.outputs.version }} is already published on crates.io"

  publish-ruviz-gpui:
    name: Publish ruviz-gpui to crates.io
    runs-on: ubuntu-latest
    needs: [check-ci, verify-packaged-crates]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Detect published ruviz-gpui version
        id: gpui-version
        run: |
          VERSION="$(cargo pkgid --package ruviz-gpui --manifest-path adapters/gpui/Cargo.toml | sed 's/.*[#@]//')"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

          if curl --silent --fail \
              --header "User-Agent: ${CRATES_IO_USER_AGENT}" \
              "https://crates.io/api/v1/crates/ruviz-gpui/${VERSION}" >/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install GPUI Linux build dependencies
        if: steps.gpui-version.outputs.published != 'true'
        uses: ./.github/actions/install-linux-desktop-build-deps

      - name: Publish ruviz-gpui to crates.io
        if: steps.gpui-version.outputs.published != 'true'
        run: cargo publish --package ruviz-gpui --manifest-path adapters/gpui/Cargo.toml --locked --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Skip ruviz-gpui publish
        if: steps.gpui-version.outputs.published == 'true'
        run: echo "ruviz-gpui v${{ steps.gpui-version.outputs.version }} is already published on crates.io"

  publish-npm-web:
    name: Publish ruviz JS SDK to npm
    runs-on: ubuntu-latest
    needs: [check-ci, publish-ruviz, publish-ruviz-web]
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Ensure wasm target is installed for the pinned toolchain
        run: rustup target add wasm32-unknown-unknown

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-web-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-web-

      - name: Cache Bun
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: ${{ runner.os }}-bun-

      - name: Install JS workspace dependencies
        run: bun install --frozen-lockfile

      - name: Check JS/TS formatting and lint
        run: bun run check:web

      - name: Validate npm package version matches release tag
        run: |
          TAG="${{ env.RELEASE_TAG }}"
          VERSION="$(python3 -c "import json; print(json.load(open('packages/ruviz/package.json'))['version'])")"

          if [ "v${VERSION}" != "${TAG}" ]; then
            echo "Release tag ${TAG} does not match npm package version v${VERSION}"
            exit 1
          fi

      - name: Validate npm tarball
        run: bun run --cwd packages/ruviz verify:pack

      - name: Detect published npm package version
        id: npm-version
        run: |
          PACKAGE_NAME="$(python3 -c "import json; print(json.load(open('packages/ruviz/package.json'))['name'])")"
          VERSION="$(python3 -c "import json; print(json.load(open('packages/ruviz/package.json'))['version'])")"
          echo "package=${PACKAGE_NAME}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

          if npm view "${PACKAGE_NAME}@${VERSION}" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish npm package
        if: steps.npm-version.outputs.published != 'true'
        # npm Trusted Publishing is configured for the `ruviz` package, so
        # GitHub OIDC supplies publish authentication and provenance here.
        run: |
          cd packages/ruviz
          if [[ "${RELEASE_TAG}" == *-* ]]; then
            npm publish --access public --provenance --tag next
          else
            npm publish --access public --provenance
          fi

      - name: Skip npm publish
        if: steps.npm-version.outputs.published == 'true'
        run: echo "${{ steps.npm-version.outputs.package }} v${{ steps.npm-version.outputs.version }} is already published on npm"

  publish-python:
    name: Publish Python package to PyPI
    runs-on: ubuntu-latest
    needs:
      - check-ci
      - build-python-sdist
      - build-python-wheel-linux
      - build-python-wheel-linux-aarch64
      - build-python-wheel-macos-intel
      - build-python-wheel-macos-arm
      - build-python-wheel-windows
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/p/ruviz
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Download Python distributions
        uses: actions/download-artifact@v4
        with:
          pattern: python-dist-*
          path: dist
          merge-multiple: true

      - name: Detect published Python package files
        id: python-version
        run: |
          python3 <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import tomllib
          import urllib.error
          import urllib.request
          from pathlib import Path

          with Path("bindings/python/pyproject.toml").open("rb") as fh:
              version = tomllib.load(fh)["project"]["version"]

          local_files = sorted(path.name for path in Path("dist").iterdir() if path.is_file())
          if not local_files:
              raise SystemExit("No Python distributions were downloaded for publishing")

          remote_files = []
          try:
              with urllib.request.urlopen(
                  f"https://pypi.org/pypi/ruviz/{version}/json",
                  timeout=15,
              ) as response:
                  payload = json.load(response)
          except urllib.error.HTTPError as exc:
              if exc.code != 404:
                  raise
          else:
              remote_files = sorted(file_info["filename"] for file_info in payload.get("urls", []))

          published = bool(remote_files) and remote_files == local_files

          print(f"version={version}")
          print(f"published={'true' if published else 'false'}")
          PY

      - name: Publish Python package
        if: steps.python-version.outputs.published != 'true'
        uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e # release/v1
        with:
          packages-dir: dist
          skip-existing: true

      - name: Skip Python publish
        if: steps.python-version.outputs.published == 'true'
        run: echo "ruviz v${{ steps.python-version.outputs.version }} is already published on PyPI"

  create-github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [check-ci, build-release, publish-ruviz, verify-packaged-crates, verify-gui-adapter-packages, publish-gui-adapters, publish-ruviz-web, publish-ruviz-gpui, publish-npm-web, publish-python]
    if: ${{ always() && needs.build-release.result == 'success' && needs.publish-ruviz.result == 'success' && needs.verify-packaged-crates.result == 'success' && needs.verify-gui-adapter-packages.result == 'success' && needs.publish-gui-adapters.result == 'success' && needs.publish-ruviz-web.result == 'success' && needs.publish-ruviz-gpui.result == 'success' && needs.publish-npm-web.result == 'success' && needs.publish-python.result == 'success' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.check-ci.outputs.release_sha }}

      - name: Prepare release notes body
        run: |
          TAG="${{ env.RELEASE_TAG }}"
          VERSIONED_NOTES_PATH="docs/releases/${TAG}.md"

          if [ -f "${VERSIONED_NOTES_PATH}" ]; then
            echo "Using versioned release notes: ${VERSIONED_NOTES_PATH}"
            cp "${VERSIONED_NOTES_PATH}" RELEASE_NOTES.md
          else
            echo "Versioned release notes not found at ${VERSIONED_NOTES_PATH}; generating fallback body."
            {
              echo "## Release ${TAG}"
              echo ""
              echo "### Changes"
              echo "See [CHANGELOG.md](CHANGELOG.md) for details."
              echo ""
              echo "### Documentation"
              echo "- View the [API Documentation](https://docs.rs/ruviz)"
            } > RELEASE_NOTES.md
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          body_path: RELEASE_NOTES.md
          draft: false
          prerelease: ${{ contains(env.RELEASE_TAG, '-') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
