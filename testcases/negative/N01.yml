name: N04-benign-whitelist
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      API_KEY: ${{ secrets.API_KEY }}
    steps:
      - run: 'curl -H "Authorization: Bearer $API_KEY" https://api.github.com/user'
