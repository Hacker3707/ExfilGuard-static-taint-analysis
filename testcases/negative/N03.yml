name: N09-mask-benign
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      API_KEY: ${{ secrets.API_KEY }}
    steps:
      - run: |
          echo "::add-mask::$API_KEY"
          echo "Secret loaded"
